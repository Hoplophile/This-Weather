<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">


    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="0"
        android:entries="@array/temp_unit_titles"
        android:entryValues="@array/temp_unit_values"
        android:key="temp_unit"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="Temperature Unit"
        android:id="@+id/temp_unit"/>

    <ListPreference
        android:defaultValue="0"
        android:entries="@array/speed_unit_titles"
        android:entryValues="@array/speed_unit_values"
        android:key="speed_unit"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="Wind Speed Unit"
        android:id="@+id/speed_unit" />

    <ListPreference
        android:defaultValue="180"
        android:entries="@array/pref_sync_frequency_titles"
        android:entryValues="@array/pref_sync_frequency_values"
        android:key="sync_freq"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_sync_frequency"
        android:id="@+id/sync_freq"/>

</PreferenceScreen>
